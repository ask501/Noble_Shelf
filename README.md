# Noble Shelf

**同人誌・電子書籍をフォルダ単位で管理し、内置ビューワーで読むためのデスクトップライブラリアプリです。**

Python と PySide6（Qt for Python）で開発された Windows 向けデスクトップアプリケーションです。ライブラリフォルダを1つ指定し、その配下の「サークル名 - 作品名」形式のフォルダや、ZIP/PDF などのファイルをスキャンして一覧表示します。サムネイル付きグリッド・サイドバーでの絞り込み・検索・お気に入り（星評価）・メタデータ編集のほか、ZIP/PDF 等は内置ビューワーでそのまま閲覧できます。メタデータ取得（検索・作品IDからの取得）はプラグインとして切り離されており、有効/無効のスイッチで本体から完全にオフにできます。

---

## Features

- **ライブラリ管理** … 1フォルダをライブラリとして登録し、サブフォルダ・アーカイブ・PDF をスキャンして DB に登録
- **グリッド表示** … サムネイル・作品名・サークル名・ページ数（または DMM/DLSite バッジ）をカード表示
- **サイドバー** … 作品名・サークル・作者・シリーズ・キャラ・タグ・お気に入り・追加順・履歴・メタデータ（プラグイン有効時）で絞り込み
- **検索** … 作品名・サークル・作者・タグ等を対象にしたテキスト検索
- **ソート・フィルター** … 作品名/サークル/追加日等でソート、ゴーストバーから複合フィルター（AND/OR）
- **内置ビューワー** … フォルダ内画像・ZIP/CBZ/7z/CB7/RAR/CBR・PDF をアプリ内で表示（1P/2P、ズーム・パン、シークバー、全画面、キーボード操作）
- **ストアファイル対応** … DMM（.dmmb/.dmme/.dmmr）・DLSite（.dlst）は専用ビュアー（ショートカット検知）で起動
- **プロパティ・メタデータ** … 作品の編集・お気に入り・カバー変更・名前変更・一括編集。プラグイン有効時は「取得」「メタデータ検索」ボタンをプラグインが配置
- **コンテキストメニュー** … 開く・プロパティ・名前変更・メタデータの取得（プラグイン有効時）・削除・除外・ショートカット作成 等
- **プラグインシステム** … メタデータ取得機能をプラグインに分離。ON/OFF で UI と処理を完全に切り替え可能。プラグインフォルダを開くメニューあり
- **設定** … フォント・テーマ・ショートカット・外部ビュアー/DMM/DLSite ビュアー・表示設定など
- **ドラッグ＆ドロップ** … フォルダ（Zip化 or そのまま登録）・アーカイブ・PDF・ストアファイルをドロップして登録
- **DB バックアップ** … 起動時の自動バックアップ（直近10件）、設定から復元可能

---

## Supported Formats

| 種別 | 拡張子 | 備考 |
|------|--------|------|
| **アーカイブ（内置ビューワー・ページ数表示）** | `.zip`, `.cbz`, `.7z`, `.cb7`, `.rar`, `.cbr` | 内包画像でページ数カウント。画像: .jpg, .jpeg, .png, .webp |
| **PDF** | `.pdf` | 内置ビューワー対応。表紙は cover_cache にキャッシュ |
| **フォルダ** | （フォルダ単位） | 画像フォルダまたは PDF 1本入りフォルダとして登録 |
| **DMM ストア** | `.dmmb`, `.dmme`, `.dmmr` | 専用ビュアー（DMMBooks.exe / DMMbookviewer.exe 等）で起動。ショートカット検知あり |
| **DLSite ストア** | `.dlst` | 専用ビュアー（DLSitePlay.exe / DLsiteViewer.exe 等）で起動。ショートカット検知あり |

---

## Project Structure

```
Noble shelf/
├── main.py              # エントリーポイント。ロギング・QApplication・フォント・アイコン設定後、MainWindow を表示
├── app.py               # メインウィンドウ (MainWindow)。メニュー・中央グリッド・サイドバー・検索バー・ステータスバー・ドロップ処理・スキャン完了時の更新
├── config.py            # 定数一元管理（APP_TITLE, APP_BASE, フォント, キャッシュパス, 拡張子定義, ショートカット既定値 等）
├── db.py                # SQLite の library.db 管理。books / bookmarks / recent_books / settings。初期化・マイグレーション・バックアップ・復元
├── grid.py              # ブックグリッド (BookGridView)。サムネ非同期ロード・カード描画・ページ数/ストアバッジ・コンテキストメニュー起動・ドラッグ
├── sidebar.py           # サイドバー (SidebarWidget)。モード（作品名/サークル/…/メタデータ/お気に入り/追加順/履歴）・絞り込み・バッジ表示
├── searchbar.py         # 検索バー。入力に応じたフィルタ（filter_books）・ハイスタックキャッシュ
├── menubar.py           # メニューバー構築。ファイル／表示／ツール／設定／プラグイン。ショートカット反映・プラグインフォルダを開く
├── context_menu.py      # グリッド用右クリックメニュー。開く・プロパティ・名前変更・メタ取得・削除・除外・ショートカット作成。ストアファイルは許可ビュアー判定
├── properties.py        # プロパティダイアログ・名前変更ダイアログ・メタ検索/適用ダイアログ・ストア追加入力・PropertyFormContext（プラグイン用ボタンコンテキスト）
├── settings_dialog.py   # 設定ダイアログ。フォント・テーマ・ショートカット・ビュアー・表示オプション・バックアップ復元
├── theme.py             # QSS とカラー定義。ダークテーマ・ボタン/カード/メニュー用スタイル
├── viewer.py            # 内置ビューワー。BookReader アダプター（Folder/Zip/7z/Rar/Pdf）。1P/2P・ズーム・パン・シーク・全画面
├── drop_handler.py      # ドラッグ＆ドロップ。フォルダ（Zip化 or そのまま）・アーカイブ・PDF・ストアファイルの登録フロー
├── scanner.py           # ライブラリスキャン。ScannerWorker が差分スキャンして DB 更新し、完了をシグナルで通知
├── filter_popover.py    # ゴーストバー用フィルタポップオーバー（AND/OR・条件追加）
├── thumbnail_crop_dialog.py  # サムネイル切り抜きダイアログ
├── plugin_loader.py     # プラグイン検出・ロード。get_plugins() / get_all_plugins() / 有効・無効の保存と取得
├── dlsite_api.py        # （本体）DLSite/FANZA 等の API 利用。プラグインから参照される場合あり
│
├── plugins/             # プラグイン配置先（メニュー「プラグイン」→「プラグインフォルダを開く」で開く）。メタ取得プラグインは同梱しない
│   └── README.md
│
├── assets/              # アイコン等（icon.png, desktop_icon.ico, バッジ画像 等）
│
└── （ユーザーデータは %APPDATA%\Roaming\NobleShelf\ に保存）
├── library.db       # SQLite DB
├── backups/         # 起動時自動バックアップ（最大10件）
├── thumb_cache/     # サムネイルキャッシュ
└── cover_cache/     # カバー画像キャッシュ
```

---

## Plugin System

- **フォルダの役割**  
  - プラグインは **`plugins/get_api/` 直下の各サブフォルダ**（`__init__.py` を持つもの）が1プラグインとして読み込まれます。  
  - `get_api` が無い場合は `plugins/` 直下のサブフォルダをスキャンします。  
  - メイン実行ファイル（exe）と同じ階層の `plugins` を参照するため、exe 化や保存場所を変えても動作します。  
  - **本リポジトリ・配布物にはメタデータ取得プラグイン（get_api）は同梱しません。** 必要に応じてユーザーが自行で配置します。

- **契約**  
  各プラグインは `__init__.py` で次のいずれかを export します。  
  - モジュール直下に `PLUGIN_NAME`, `PLUGIN_SOURCE_KEY`, `search_sync`, `get_metadata_sync` を定義する  
  - または `get_api()` を定義し、上記属性を持つオブジェクトを返す  

  任意: `can_handle(product_id_or_url)`, `get_property_buttons(context)`（プロパティ／ストア追加ダイアログに並べるボタン）。

- **有効/無効の仕組み**  
  - **保存**: 設定キー `plugin_enabled_<PLUGIN_SOURCE_KEY>` を DB の `settings` に `"1"`（有効）または `"0"`（無効）で保存します。  
  - **取得**: `plugin_loader.is_plugin_enabled(source_key)` が未設定の場合は有効、明示的に `"0"` のとき無効。  
  - **API の使い分け**:  
    - **`get_plugins()`** … 有効なプラグインのみ返す。検索・メタ取得・コンテキスト「メタデータの取得」・プロパティの取得/メタ検索ボタン・サイドバー「メタデータ」はすべてここだけを使うため、無効にするとこれらの UI と処理が一切出ません。  
    - **`get_all_plugins()`** … 有効/無効を問わず読み込み済みプラグインを返す。メニュー「プラグイン」の一覧表示（チェック ON/OFF）に使用します。  
  - プラグインを同梱しないため、配布時は最初からメタデータ取得関連の UI は出ず、「スクレイピング機能なし」の状態で提供できます。

---

## Usage

### 起動方法

- **Python で実行する場合**  
  - 依存をインストールしたうえで、プロジェクトルートで:  
    `python main.py`  
  - 初回またはライブラリ未設定時は、起動後に「ライブラリフォルダを設定」でフォルダを選ぶとスキャンが始まります。

- **exe 化する場合**  
  - PyInstaller 等で `main.py` を exe にし、生成された exe と同じフォルダに空の `plugins` フォルダ（必要なら `assets`）を置けば、保存場所を変えても動作します。メタ取得プラグインは同梱しないため、`plugins/get_api/` は含めずに配布します。

### ライブラリフォルダの設定手順

1. メニュー **「ファイル」** → **「ライブラリフォルダを設定」** を選択する。  
2. 同人誌・電子書籍を格納しているフォルダ（サブフォルダに「サークル名 - 作品名」形式のフォルダや ZIP/PDF がある想定）を選択して **「フォルダの選択」** をクリック。  
3. スキャンがバックグラウンドで実行され、完了するとグリッドに一覧が表示されます。  
4. 以降の起動時は、保存済みのライブラリフォルダで自動的にスキャンされます。  
5. **「ファイル」** → **「ライブラリを開く」** で、設定中のライブラリフォルダをエクスプローラーで開けます。

---

## Screenshots

<!-- スクリーンショットを追加する場合はここに画像を配置してください。 -->
<!-- 例: ![メイン画面](docs/screenshot_main.png) -->

---

## Third-party / Dependencies

本プロジェクトは以下のライブラリ等を使用しています。各ライセンスはそれぞれの著作権者に帰属します。

| 名前 | 用途 | ライセンス |
|------|------|------------|
| [PySide6](https://doc.qt.io/qtforpython/) (Qt for Python) | GUI | [LGPL v3](https://www.gnu.org/licenses/lgpl-3.0.html) 等 |
| Python | 実行環境 | [PSF License](https://docs.python.org/3/license.html) |

その他、PyMuPDF (fitz)、Pillow、py7zr、rarfile 等の依存も利用しています。詳細は各パッケージの表記に従います。

---

## License

本リポジトリのコードは **MIT License** です。Copyright (c) 2026 ask501。
全文は [LICENSE](LICENSE) を参照してください。

